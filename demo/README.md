# demo

## About

A Docker Compose configuration for running application demo.

## Setup

Install Make and Docker. Use the following command:

```bash
make up     # Run application.
make down   # Shutdown application.
```
