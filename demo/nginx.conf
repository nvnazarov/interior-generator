events {
}

http {
    log_format debug '$request $account_id';
    access_log /var/log/nginx/access.log debug;

    map $http_origin $cors_origin {
        default "";
        "http://localhost:3000" $http_origin;
        "http://127.0.0.1:3000" $http_origin;
    }

    upstream projects {
        server projects:80;
    }
    upstream exporter {
        server exporter:80;
    }
    upstream generator {
        server generator:80;
    }
    upstream catalog {
        server catalog:80;
    }
    upstream auth {
        server auth:80;
    }
    upstream minio {
        server minio:9000;
    }
    upstream frontend {
        server frontend:80;
    }

    server {
        listen 80;

        location = /_auth {
            internal;
            proxy_pass_request_body off;
            proxy_set_header Content-Length "";
            return 204;
        }

        # Account management.
        location /api/auth/ {
            if ($request_method = OPTIONS) {
                add_header Access-Control-Allow-Origin $cors_origin always;
                add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
                add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
                add_header Access-Control-Allow-Credentials "true" always;
                add_header Access-Control-Max-Age 86400 always;
                add_header Content-Length 0 always;
                return 204;
            }
            add_header Access-Control-Allow-Origin $cors_origin always;
            add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
            add_header Access-Control-Allow-Credentials "true" always;

            proxy_pass http://auth;
        }

        # Object storage.
        location /assets/ {
            proxy_pass http://minio;
            rewrite ^/assets(/.*)?$ $1 break;
        }

        # Catalog service.
        location /api/catalog {
            proxy_pass http://catalog;
            rewrite ^/api/catalog(/.*)?$ $1 break;
            auth_request /_auth;
            auth_request_set $account_id $upstream_http_x_account_id;
            proxy_set_header x-account-id "f51a3baa-1e2b-4e42-b7f6-b2d5dcf01200";
        }

        # Exporter service.
        location ~ ^/api/projects/([0-9A-Za-z\-]+)/export/pdf$ {
            if ($request_method = OPTIONS) {
                add_header Access-Control-Allow-Origin $cors_origin always;
                add_header Access-Control-Allow-Methods "POST, OPTIONS" always;
                add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
                add_header Access-Control-Allow-Credentials "true" always;
                add_header Access-Control-Max-Age 86400 always;
                add_header Content-Length 0 always;
                return 204;
            }
            add_header Access-Control-Allow-Origin $cors_origin always;
            add_header Access-Control-Allow-Methods "POST, OPTIONS" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
            add_header Access-Control-Allow-Credentials "true" always;

            proxy_pass http://exporter;
            rewrite ^/api(/.*)?$ $1 break;
            auth_request /_auth;
            auth_request_set $account_id $upstream_http_x_account_id;
            proxy_set_header x-account-id "f51a3baa-1e2b-4e42-b7f6-b2d5dcf01200";
        }
        location ~ ^/api/plans/([0-9A-Za-z\-]+)/export/dxf$ {
            proxy_pass http://exporter;
            auth_request /_auth;
            auth_request_set $account_id $upstream_http_x_account_id;
            proxy_set_header x-account-id "f51a3baa-1e2b-4e42-b7f6-b2d5dcf01200";
        }

        # Projects service.
        location = /api/projects {
            if ($request_method = OPTIONS) {
                add_header Access-Control-Allow-Origin $cors_origin always;
                add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
                add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
                add_header Access-Control-Allow-Credentials "true" always;
                add_header Access-Control-Max-Age 86400 always;
                add_header Content-Length 0 always;
                return 204;
            }
            add_header Access-Control-Allow-Origin $cors_origin always;
            add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
            add_header Access-Control-Allow-Credentials "true" always;

            proxy_pass http://projects;
            rewrite ^/api(/.*)?$ $1 break;
            auth_request /_auth;
            auth_request_set $account_id $upstream_http_x_account_id;
            proxy_set_header x-account-id "f51a3baa-1e2b-4e42-b7f6-b2d5dcf01200";
        }
        location ~ ^/api/projects/([0-9A-Za-z\-])+(/publish|/unpublish|/plans)?$ {
            if ($request_method = OPTIONS) {
                add_header Access-Control-Allow-Origin $cors_origin always;
                add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
                add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
                add_header Access-Control-Allow-Credentials "true" always;
                add_header Access-Control-Max-Age 86400 always;
                add_header Content-Length 0 always;
                return 204;
            }
            add_header Access-Control-Allow-Origin $cors_origin always;
            add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
            add_header Access-Control-Allow-Credentials "true" always;

            proxy_pass http://projects;
            rewrite ^/api(/.*)?$ $1 break;
            auth_request /_auth;
            auth_request_set $account_id $upstream_http_x_account_id;
            proxy_set_header x-account-id "f51a3baa-1e2b-4e42-b7f6-b2d5dcf01200";
        }
        location ~ ^/api/plans/([0-9A-Za-z\-]+)$ {
            proxy_pass http://projects;
            auth_request /_auth;
            auth_request_set $account_id $upstream_http_x_account_id;
            proxy_set_header x-account-id "f51a3baa-1e2b-4e42-b7f6-b2d5dcf01200";
        }

        # Generator service.
        location ~ ^/api/projects/([0-9A-Za-z\-])+/plans/generate$ {
            if ($request_method = OPTIONS) {
                add_header Access-Control-Allow-Origin $cors_origin always;
                add_header Access-Control-Allow-Methods "POST, OPTIONS" always;
                add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
                add_header Access-Control-Allow-Credentials "true" always;
                add_header Access-Control-Max-Age 86400 always;
                add_header Content-Length 0 always;
                return 204;
            }
            add_header Access-Control-Allow-Origin $cors_origin always;
            add_header Access-Control-Allow-Methods "POST, OPTIONS" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
            add_header Access-Control-Allow-Credentials "true" always;

            proxy_pass http://generator;
            rewrite ^/api(/.*)?$ $1 break;
            auth_request /_auth;
            auth_request_set $account_id $upstream_http_x_account_id;
            proxy_set_header x-account-id "f51a3baa-1e2b-4e42-b7f6-b2d5dcf01200";
        }
        
        # Frontend.
        location /app {
            proxy_pass http://frontend;
        }
    }
}